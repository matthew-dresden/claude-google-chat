# Fixture missing the required space_id (used to assert fail-fast behavior).
webhook_url = "https://chat.googleapis.com/v1/spaces/AAAA/messages?key=SECRETKEY"
oauth_client_file = "/tmp/client_secret.json"
